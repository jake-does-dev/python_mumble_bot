deploy/.env
__pycache__/
*.pyc
.venv/
*.egg-info/
build/
.pytest_cache/
data/
# Runtime clip directories — but NOT the pmb_core.audio Python package, which
# the bots import (midi/transform) and must be in the repo.
audio/
!pmb_core/pmb_core/audio/
